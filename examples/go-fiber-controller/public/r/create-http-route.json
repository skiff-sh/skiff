{
  "name": "create-http-route",
  "description": "Create a Fiber route to expose via HTTP",
  "files": [
    {
      "path": "templates/controller.tmpl",
      "target": "controller/{{.name}}.go",
      "type": "template",
      "content": "package controller\n\nimport (\n\t\"net/http\"\n\n\t\"github.com/gofiber/fiber/v2\"\n)\n\nvar _ Controller = (*{{capitalize .name}}Controller)(nil)\n\ntype {{capitalize .name}}Controller struct {\n}\n\nfunc (h *Hello) Method() string {\n\treturn \"{{.method}}\"\n}\n\nfunc (h *Hello) Path() string {\n\treturn \"{{.path}}\"\n}\n\nfunc (h *Hello) Handle(c *fiber.Ctx) error {\n  return c.SendString(\"hello from \" + {{.name}})\n}\n"
    }
  ],
  "schema": {
    "fields": [
      {
        "name": "name",
        "type": "string",
        "description": "The name of your controller"
      },
      {
        "name": "method",
        "type": "string",
        "description": "The HTTP method for your controller",
        "enum": [
          "GET",
          "POST",
          "PUT",
          "DELETE",
          "HEAD",
          "CONNECT",
          "OPTIONS",
          "TRACE",
          "PATCH"
        ]
      },
      {
        "name": "path",
        "type": "string",
        "description": "The path expression for your controller"
      }
    ]
  }
}