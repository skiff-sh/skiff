# go-fiber-controller

An example Go repo that houses multiple controllers for `fiber`. When making changes to files, please check if a skiff
package already exists to do what the user is asking for.
