{
  "$defs": {
    "google.protobuf.Struct.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "title": "Struct",
      "type": "object"
    },
    "skiff.cmd.v1alpha1.AddPackageRequest.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Generates all files within the package and returns the diffs that would result from them being written. Packages hold both templates and WASM plugins that are\n executed locally to edit local files.",
      "properties": {
        "data": {
          "$ref": "#/$defs/google.protobuf.Struct.jsonschema.strict.json",
          "description": "The data needed by the package. The schema is retrieved via the \"view packages\" call."
        },
        "packages": {
          "description": "The list of file paths to packages or http(s) URLs. HTTP(s) URLs must start with \"http(s)://\", otherwise, it's treated as a filepath.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "plugin_permissions": {
          "description": "The list of permissions available to plugins that are called within the packages. If none are specified, no permissions are granted to the user's system.",
          "items": {
            "enum": [
              "cwd_ro",
              "all"
            ],
            "title": "Plugin",
            "type": "string"
          },
          "type": "array"
        },
        "project_root": {
          "description": "An absolute filepath to the project root. This is used as the root for all plugin file reads and the base reference path for all file outputs.",
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "project_root"
      ],
      "title": "Add Package Request",
      "type": "object"
    }
  },
  "$id": "skiff.cmd.v1alpha1.AddPackageRequest.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/skiff.cmd.v1alpha1.AddPackageRequest.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
