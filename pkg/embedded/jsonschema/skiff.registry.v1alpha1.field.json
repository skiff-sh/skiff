{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "default": {
      "description": "The default value. If set, the field will be marked as optional. If the enum field is set, this field must be one of the enum values.",
      "title": "Value"
    },
    "description": {
      "description": "A short description of the field. This is highly recommended to provide context to all consumers of your package.",
      "type": "string"
    },
    "enum": {
      "description": "Restrict your options to a set of values to choose.",
      "title": "List Value",
      "type": "array"
    },
    "items": {
      "additionalProperties": false,
      "description": "If the type is set to \"array\", this specifies the underlying type. Required if type is \"array\".",
      "properties": {
        "enum": {
          "description": "Restrict your options to a set of values to choose.",
          "title": "List Value",
          "type": "array"
        },
        "type": {
          "description": "Required. Can be a string or number.",
          "enum": [
            "string",
            "number"
          ],
          "title": "Type",
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "title": "Sub Field",
      "type": "object"
    },
    "name": {
      "description": "The name of the field. This must be unique between all fields.",
      "minLength": 1,
      "type": "string"
    },
    "type": {
      "description": "The expected type of the field.",
      "enum": [
        "string",
        "bool",
        "number",
        "array"
      ],
      "title": "Type",
      "type": "string"
    }
  },
  "required": [
    "name",
    "type"
  ],
  "title": "Field",
  "type": "object"
}
