{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Generates all files within the package and returns the diffs that would result from them being written. Packages hold both templates and WASM plugins that are\n executed locally to edit local files.",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "data": {
      "additionalProperties": {
        "title": "Value"
      },
      "description": "The data needed by the package. Should be a simple map of the field name to the value.",
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "package": {
      "description": "The file path or http(s) URLs to the package. HTTP(s) URLs must start with \"http(s)://\", otherwise, it's treated as a filepath.",
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "package"
  ],
  "title": "Add Package Request",
  "type": "object"
}
